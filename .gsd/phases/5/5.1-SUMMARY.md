# 5.1 Summary

## Completed Tasks
- **Support Data Services:** Created `support.service.js` to handle API interactions for `Portales`, `Puestos`, and `Titulos`. Data fetching uses `axios` and injects JWT authorization headers via `auth.service.js`.
- **Postulante Service (CRUD & Files):** Developed `postulante.service.js` implementing a comprehensive CRUD wrapper. It handles advanced query parameter passing for search, file uploads through `FormData`, and file downloading via Blob to correctly prompt the browser save dialog.

## Verifications run
- `web/src/services/support.service.js` contains methods `getPortales`, `getPuestos`, `getTitulos`.
- `web/src/services/postulante.service.js` handles file uploads with `multipart/form-data` and downloads with `responseType: 'blob'`.

## Next steps
- Plan 5.2: Postulantes List & Advanced Search.
