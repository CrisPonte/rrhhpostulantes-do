---
phase: 5
plan: 2
wave: 2
---

# Plan 5.2: Postulantes List & Advanced Search

## Objective
Build the main entry point to manage applicants. Include a robust data table and advanced filter controls (Provincia, Título, Puesto, Estado, Fechas) per the specification.

## Context
- `web/src/pages/PostulantesList.jsx`
- `.gsd/SPEC.md`
- `web/src/services/postulante.service.js`

## Tasks

<task type="auto">
  <name>Advanced Filter Form</name>
  <files>
    - web/src/components/SearchFilters.jsx
    - web/src/pages/PostulantesList.jsx
  </files>
  <action>
    - Create `SearchFilters.jsx` accepting `onSearch(filters)` callback.
    - Fields: `nombre`, `apellido`, `dni`, `provincia` (select array from RFP), `puesto` (from support service), `tituloPrincipal` (from support service), `estadoEntrevista` (select array from RFP: "Entrevista", "Desestimado", etc).
    - Provide a "Limpiar" button.
  </action>
  <verify>grep "SearchFilters" web/src/pages/PostulantesList.jsx</verify>
  <done>Advanced filtering component visually matches complex querying logic.</done>
</task>

<task type="auto">
  <name>Data Grid & PostulantesList Page</name>
  <files>
    - web/src/pages/PostulantesList.jsx
  </files>
  <action>
    - Fetch list from `postulanteService.getAll(filters)`.
    - Display in a responsive Tailwind HTML `table` with columns: Nombre, DNI, Puesto, Título, Estado Entrevista, Acciones.
    - Acciones column: View/Edit (Link to `/postulantes/:id`) and Delete (Soft delete button for Jefe/Admin, Hard delete visually separated for Admin).
    - Map the route in `App.jsx` pointing to this component if not already pointing to realistic UI.
  </action>
  <verify>grep "table" web/src/pages/PostulantesList.jsx</verify>
  <done>Postulantes grid presents data interactively.</done>
</task>

## Success Criteria
- [ ] Users can browse applicants conditionally.
- [ ] Table layout is clear, concise, and styled consistently.
- [ ] Support data populates the dropdown selectors dynamically.
